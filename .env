# Base configuration for Amano Discord Bot
# This file contains safe defaults and is committed to the repository
# Use .env.local for local development overrides (ignored by git)

# Application Settings
MAX_MESSAGE_LIMIT=20
